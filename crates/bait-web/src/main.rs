/// bait-web placeholder.
///
/// The full web UI (repository browser, commit history, narada review interface)
/// will be built with Leptos (Rust WASM) in a future phase.
fn main() {
    println!("bait-web is not yet implemented.");
    println!("The HTTP API is served by bait-server on port 7979.");
    println!("A full browser UI is planned for a future release.");
}
